server {
    listen 80;
    listen [::]:80;

    server_name console.latenzo.aspltools.in www.console.latenzo.aspltools.in;

    # Redirect www → non-www (optional but recommended)
    if ($host = www.console.latenzo.aspltools.in) {
        return 301 http://console.latenzo.aspltools.in$request_uri;
    }

    location / {
        proxy_pass http://127.0.0.1:3001;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 90s;
        proxy_send_timeout 90s;
    }
}




    sudo ln -s /etc/nginx/sites-available/webworker.latenzo.aspltools.in /etc/nginx/sites-enabled/


sudo certbot --nginx -d webworker.latenzo.aspltools.in -d www.webworker.latenzo.aspltools.in
